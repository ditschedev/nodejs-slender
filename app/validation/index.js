const authRules = require('./auth');
const groupRules = require('./groups');
const postRules = require('./posts');
const userRules = require('./users');

module.exports = {
    authRules,
    groupRules,
    postRules,
    userRules
};